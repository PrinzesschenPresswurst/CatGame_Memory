using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class todo : MonoBehaviour
{
    // why doesnt make noce rows with 12 - review row descision
    // make a pülaceholder when is uneven (like with 9)
    // gameplay rounds (pick 2 cards)
    // compare if picked cards are same
    //
}